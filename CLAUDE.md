See @AGENTS.md for the project guide (stack, file layout, recipe-editing
and design rules, commands). It is the single source of truth so
instructions don't drift between tools — keep edits there, not here.

Repo-specific subagents live in `.claude/agents/`.
