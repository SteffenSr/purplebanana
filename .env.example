# Enables api/chat.mts to run the OpenAI Agents SDK for the "Nomi" chatbot
# experiment (src/app/experiments/chatbot). Without it, that endpoint
# returns mocked replies instead of calling a real model — see api/chat.mts.
OPENAI_API_KEY=
